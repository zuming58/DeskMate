# DeskMate design direction

T36 (2026-09-13): 用户提供四段 4:3 居家视频，替代被否决的人物两帧眨眼。陪伴页按等待/倾听/思考/实际 PCM 播放切换无声视频，双层首帧就绪后 150ms 淡入淡出；打断立即停嘴，离页卸载、后台/不可见释放，不影响语音链路。人物框保持 4:3，最大 570×427.5；侧栏深色外框、透明小脸和真实连接状态不变。不是逐字口型。详见 `docs/design/companion-state-video-t36.md`。

T35A (2026-09-13): 按用户补充要求恢复侧栏原有深色圆角外框；仅内部脸部素材透明、下方只显示连接状态。人物两帧眨眼已被用户否决，默认停用，保留选中静态图等待用户提供 15 秒循环视频。交付必须启动带正式 preload 和原用户配置的完整程序，不以禁用按钮、默认姓名的隔离外观预览代替。

T35 (2026-09-13): 陪伴页采用用户选定的蓝衬衫居家书桌人物，保留 3:2 构图并仅在眼睛区域切换闭眼帧；离页、不可见或减少动态时停止眨眼。这是轻量视觉预览，不冒充实时口型。侧栏青色小脸改为透明素材，直接融入石墨背景，下面只保留真实连接状态。D/M 品牌、语音与实体屏幕不变。详见 [T35 人物预览](docs/design/companion-home-preview-t35.md)。

T34B (2026-09-13): 接受语音启动后立即显示“正在准备录音”，不等麦克风初始化完成才出现胶囊；准备中不冒充录音，真正开始采集后才显示录音波形与计时。Esc可取消等待，不抢目标窗口焦点。

T34/T34A (2026-09-13): 按键页统一“管理场景”，支持改名、说明、排序与可恢复归档；两页场景状态一致。记忆页先展示结果，连接、清理、索引及危险操作收进高级区。设置编辑提供草稿保护、取消和失败反馈；窄屏设置导航改为顶部网格。备份页明确区分预览、手动有效快照和确认重启恢复。听写只在整段流式会话结束后写入，异常使用完整录音重识别，避免前半句被误当整段。验收与边界见 `docs/reviews/t34-software-closure-2026-09-13.md`。

T33 (2026-09-13): 记忆管理增加“本地自动清理”。录音默认 7 天、原始文字默认 20 天并可调整；首次必须先看精确预览再确认，修改期限会撤销旧授权。达到期限后仍需满足成功转写、可靠日期、日终总结和必要的 KnowledgeOS 两类回执；未满足项显示保留原因。内部恢复任务保留 7 天，手动备份、摘要、长期记忆和 KnowledgeOS Raw 不清理。详见 [T33 交付说明](docs/reviews/t33-consent-retention-2026-09-13.md)。

T32B (2026-09-12): 设置增加“数据与备份”，明确私人备份与脱敏诊断的区别；录音默认不勾选，导出/校验提供执行中、失败重试和条目预览。预览不恢复，未完成重启交接与防重发前不展示可点击的覆盖恢复按钮。保留原配置导入导出，不把它们误标为完整数据恢复。

T32 (2026-09-12): 历史页显示持久化/迁移状态和失败重试，未知日期明确标记。迁移期“移除历史”只移除活动列表，原副本与受管录音暂留，确认框不得声称物理删除。配置损坏显示保护警告，不用默认值覆盖原数据；自动清理暂停，等待统一预览与首次确认。完整备份恢复和全产品界面收尾仍待开发。

T31 (2026-09-12): 配置操作与用户内容分离；恢复软件设置不删除历史。历史导出、词库导入导出提供真实文件与确认，自动保存状态可见；记忆编辑不被刷新覆盖。删除演示提醒、假音效/蓝牙开关及未接入的主题/透明度控件。实际输入服务状态取代固定绿灯；原始记录清理明确存储范围。详见 [修复报告](docs/reviews/t31-audit-fixes-2026-09-12.md)。

T30 (2026-09-12): 同一自然日的聊天连续；跨天普通问候不自动带入昨天闲聊，主动询问历史时才检索。工作回顾同时查陪伴与语音输入，并明确口述素材不等于本人经历。识别已开始但未完成时使用独立等待窗口，超时提示重说；插入诊断区分目标变化、输入失败和主动复制模式。
T37 (2026-09-14): 只在“AI 陪伴”下方新增“风格映像”，不替换既有侧栏、Logo、页头或其他页面。新页采用用户选定的横向浅色进片机、纯白工作区、上方素材/下方作品、围绕旋钮的弧形风格卡片；强度紧凑显示在旋钮上方，S1 进入调整。当前为明确标识的本地样片交互，真实 AI 与硬件接管待后续接入。详见 `docs/design/style-studio-t37.md`。


T26A (2026-09-11): 用户选定蓝青色折带 D/M、平整暖白圆角方形应用图标，不使用玻璃底座或额外黑框。品牌与陪伴形象分开：顶部品牌、工作台及原生图标统一 D/M；实时陪伴和左下角设备卡采用浅蓝柔和形象、浅色背景与深青眼睛，支持自然睁闭眼。仅改变软件视觉，不修改实体屏幕或声音。详见 [品牌与陪伴形象](docs/design/deskmate-identity-t26a.md)。

T26B (2026-09-11): Codex 项目状态不显示或播报纯十六进制/UUID 临时工作树目录。优先自动解析 Git 仓库名，再采用稳定目录或任务标题；解析完成前统一称为“Codex 临时任务”，后续目录刷新可自动改为真实项目名。新项目仍由生命周期 Hook 自动发现，无需手工添加。

T26 (2026-09-11): 工作台改为真实数据综合看板：相伴/今日使用/已确认记忆、连接服务、7 天使用、小时→日终→KnowledgeOS、当前场景与项目状态。大脸缩为小型标识，不再占用首页。详情和配置留在原页面，首页只读概览和导航；“已配置”不冒充“已连接”，无记录与读取失败明确区分。详见 [工作台设计与统计口径](docs/architecture/workbench-overview.md)。

T22F (2026-09-11): keep prompt ordering explicit and local: adjacent up/down controls appear only in the unfiltered current-scene list. Scene KEY5–7 use direct common-action choices, plus custom shortcut, copy-only prompt, registered application and disabled. Opening an application must reuse the existing whitelist and never expose a raw command field.

T22E (2026-09-11): prompt rotary selection must not require hovering over the list. Keep the 45/55 fixed panes; source-identified wheel input only changes foreground selection. Key mapping shows a compact KEY8 paste repair when its assignment is not paste, leading to existing synchronization confirmation. Do not add a second keyboard editor to Prompts.

## Product feeling

DeskMate 不是传统硬件调试器，而是一个安静、可信、带有生命感的 AI 工作伙伴。界面应让第一次使用者无需理解 GPIO、HID 报告或网络协议，也能完成语音输入、查看状态和配置设备。

## Visual language

- 导航：深石墨色，层级稳定，图标在 16–24 px 下清晰。
- 工作区：浅灰或暖白，不使用整页纯黑。
- 强调色：青蓝与钴蓝；绿色仅表示健康连接，琥珀表示等待，红色表示错误。
- 卡片：少量圆角、轻边框和克制阴影，避免过度玻璃拟态。
- 提示词：只在场景栏使用低透明度磨砂和细边框，正文列表与编辑器保持白底；复用现有导航、Tabler 图标和钴蓝选择态，不增加第二套应用外壳。
- 字体：中文优先系统黑体或 Noto Sans SC，拉丁字符使用清晰的无衬线字体。
- 品牌：暖白方形底、蓝青 D/M，不叠加黑色外框；软件陪伴：浅蓝柔和形象与浅色背景，深青眼睛，不用黑色机械外壳。

## Interaction priorities

1. 录音是否开始、麦克风是否收到声音、当前识别到什么，必须即时可见。
2. 语音页必须明确显示已保存的麦克风来源和本次实际锁定来源；板载麦克风开始前不可用时，必须展示回退原因，不能把电脑麦克风伪装成板载麦克风。
3. 悬浮条底部居中、单行、紧凑、半透明、不抢输入焦点。
4. 设备连接、语音服务、输出状态分别展示，不把网络可用误写成板载麦克风已连接。
5. 高风险操作需要明确确认；失败必须保留原文和历史。
6. 模拟状态与真实硬件状态要有清晰标签。
7. 工作台不得显示固定传感器数值、虚构姿态或相对时间来冒充实时硬件；只能展示已有诊断能够证明的连接状态，未接入能力明确标为待接入或未启用。
8. 记忆管理必须把审核、纠正、导出和彻底忘记做成显式动作。知识库只显示是否配置和目录名，完整路径留在 Electron 主进程；Markdown 双链接和 embedding 是可重建投影，不能冒充 SQLite 真源。
9. 实时陪伴大脸必须首先显示真实会话状态。电脑扬声器回答时明确显示“回答中 · 防回声”，自动语音打断暂停，手动打断始终可见。
10. 后台本地唤醒不显示悬浮胶囊；只有唤醒命中或用户明确启动前台陪伴后才显示。前台连续对话在监听空闲 10 秒后退出并恢复后台唤醒。
11. 语音唤醒成功后先用当前陪伴音色简短回应用户称呼，然后进入聆听；停止监听不等于清空上下文。近期陪伴对话由主进程保留，跨唤醒、重连与软件重启接续；较早的近期记录按问题检索，长期事实仅检索已审核记忆。
12. 记忆管理提供“整理待处理记录”和“打开笔记文件夹”。原文即时落盘，支持关机后手动补整理多天内容；日期命名的 Markdown 默认存在软件内置知识库，后续可改选外部目录。整理过滤杂谈但不改写原文，不扫描其他资料。
13. AI 陪伴总览把用户明确资料与陪伴人格并列放在主内容区，使用一个保存入口，避免在窄侧栏重复堆叠和长距离滚动。用户年龄、职业等明确字段优先于旧对话中的未知回答；陪伴年龄属于人格设定，不得自行编造。

## Core surfaces

- Workbench：真实使用与记忆概览、软硬件服务状态、当前场景和可信项目状态；小型陪伴标识，不放大脸、假任务百分比或未开发传感器占位。
- Voice：录音、实时字幕、转写、整理和输出闭环。
- Companion：实时大脸与陪伴/设备证据优先；实体动作和智能控制为后续分区。智能控制只维护明确授权的应用白名单与 Codex 可信状态，不展示其他 Agent 入口。
- History：原始转写与整理结果、复制、导出和删除。
- Vocabulary：热词、专业名词和确定性替换规则。
- Prompts：统一提示词场景与第 5～7 键映射；Tab/Shift+Tab 切换场景并短句播报。推荐第 4 键调出主窗口提示词页，再次按下复制选中内容并收起，第 8 键粘贴；共用键允许用户重新配置，提示标明默认。Esc 只收起快捷调出的临时页，手动浏览和语音取消不隐藏主窗口。
- Key mapping：顶部大号场景按钮，紧接八键与旋钮实物图，右侧仅一个所选按键编辑器。1、2、3、4、8 全局共用，5～7 按场景独立；常用动作直接选择，自定义快捷键仍可录入，打开应用只选已登记白名单。场景按钮与提示词页 Tab 共用当前场景。不得再增加平行的场景下拉框和三组按键表单。同步入口明确可见，播报、选词方向、推荐方案和诊断收进底部折叠区。
- Memory：作为左侧独立页面统一管理陪伴对话与语音输入，SQLite 原文、审核候选、摘要、搜索与知识库投影各自有明确边界。
- Device：HID、麦克风、网络和已有固件能力，作为“设备与诊断”内的设备连接子页。
- Settings and diagnostics：设备连接、快捷键、输出、外观、AI 服务、安全和脱敏诊断。

T23：设置提供“启用小智硬件扩展”总开关及“仅使用 EasyInput”说明。关闭后收起或禁用实体屏幕、表情、舵机与舞蹈入口，显示“已关闭”而非故障；AI 陪伴页面、软件会话形象、语音输入与办公功能保留。该开关不等于关闭 AI 伴侣，也不等于硬件断电。详见 [任务说明](flow/tasks/t23-optional-xiaozhi-hardware.md)；代码已实现，真机验收仍待完成。

Codex 工作状态默认显示在 EasyInput 的五颗灯上，不长期占用小智表情。五灯使用同一低亮度状态底色：聆听蓝、思考紫、工作青、等待琥珀、完成绿、错误红，空闲熄灭；按键与旋钮反馈可短暂覆盖并在结束后恢复。只有真实 Hook 和固件显式能力声明能驱动，详见 [T24 合同](docs/contracts/t24-codex-led-status-v1.md)。

主导航固定为工作台、语音输入、AI 陪伴、历史记录、词库、提示词、按键配置、记忆管理、设备与诊断。设备连接收进设备与诊断；智能控制和动作编排收进 AI 陪伴。表情资源继续复用，但 Companion Overview 不提供独立预览入口；表情编辑、环境感知、人脸跟随、人物/声纹档案和其他 Agent 不作为当前产品入口。

动作声音由电脑扬声器播放，小智只执行已验收语义动作。关注、点头、寻找使用短小的内置合成音效；跳舞在没有本地歌曲时使用内置电子节拍，用户也可显式选择一首本地歌曲覆盖。文件路径在 Electron 主进程使用 Windows 加密存储，渲染层只看到文件名和有界音频字节；结束、停止或急停必须停止配乐。通用音乐播放与流媒体控制已取消；这里只保留与舞蹈生命周期绑定的本地/电子配乐，不引入新的固件或音频下行。

提示词页固定在可用窗口高度内，左侧约 45% 为标题与简短说明的快选，右侧约 55% 为完整正文阅读。正文使用清晰对比与较大字号，两栏独立框内滚动；选词不带动顶部场景栏或外层页面。推荐第 3 键呼叫语音助手、第 4 键用于提示词、第 8 键粘贴；这些共用键一处修改、所有场景共用，不随场景切换。旧版第 3/4 键布局首次升级时自动填入上述两项本机修改，键帽明确标为“待同步”；后续自定义不重复覆盖。硬件更改仍需在按键配置预览并确认；本机未同步修改不得被重新读取的板上旧配置覆盖。

提示词顺序属于每个工作场景的本地资料布局。只在“当前场景 + 全部 + 无搜索/分类”的稳定列表显示相邻上移/下移；搜索、收藏、最近、我的、回收站和跨场景浏览不显示排序，避免把临时结果顺序误写成主顺序。排序后继续选中被移动项，并在重启及备份导入导出后保留。

按键配置页也使用 Tab/Shift+Tab 正向/反向循环场景，不逐个跳到八个实体键或工具按钮；当前选中的实体键保持不变。右侧编辑区、快捷键录制、输入法组字和弹窗保留正常键盘操作。异步切场景期间按钮可能临时禁用，焦点恢复必须等按钮真正重新启用，避免下一次 Tab 退回原生遍历。

提示词动作的键面短名为“弹出/收起”，固定单行，不因长名称撑高按键；完整功能保留在悬停标题、无障碍名称和右侧编辑器。顶部同步提示把“键盘配置已读取”“本机修改待同步”“已同步并回读确认”分开，读取成功不等于本机修改已写入；失败或回读未确认时不得清除待同步标记。

AI 服务设置分为“语音转写”“文本大模型”和“实时语音”三块。文本大模型统一供应智能整理与可选语音编辑，并作为意图 Bridge、每日摘要和记忆候选提取的共享适配器；陪伴主链的具体供应商和三段式流式边界以当前阶段合同为准，不直接由语音供应商执行 Windows 动作。

T26A 将品牌与陪伴形象明确分离。`BrandLogo` 是静态 D/M；`CompanionFace` 保持一个渲染组件，通过 `appearance="soft"` 为实时陪伴和左下角设备脸提供浅蓝睁闭眼素材。默认 classic 七张历史图片保留给既有动作/表情预览，避免改变硬件表情标识。软件脸由陪伴会话状态驱动，不再将 Codex 工作状态或手工历史表情当成实时对话状态。自然眨眼在隐藏页面与减少动画偏好下停止。

参考资产见 `design/`，仅借鉴信息结构与交互，不复制 EasyInput 品牌或视觉实现。
