# T03 EasyInput USB input runtime provenance

All T03 implementation files are clean-room reimplementations. No source file, generated file, binary, or build output was copied from an external project.

| Product file | Fixed reference and license | Adoption and target |
| --- | --- | --- |
| input_core include and source files | EasyInput Maker 7619bd13f9ddfd6e2d80e2b8e022ef0acf32ce01, PolyForm Noncommercial 1.0.0; ESP-IDF v5.5.5 Apache-2.0 | Debounce and Gray-code behavior independently reimplemented from the frozen contract; split key/phase/press entry points, bounded event ring, overflow accounting, explicit pending-sequence discard after a drop, encoder resync, and a read-only debounced eight-key physical mask for the existing runtime owner. |
| hid_report include and source files | Maker fixed commit above; PolyForm Noncommercial 1.0.0; HID boot convention | Independent held-key state with modifier + apple_fn + six usages, fail-closed rollover and complete release. |
| input_runtime include and source files | Frozen INPUT_V1_FROZEN contract; Maker main/platform/usb_hid.cpp and keyboard queue/lifetime tests read with git show at fixed commit | Clean reimplementation of default action ownership, encoder axis routing, 16-item HID queue, mount epochs, disconnect/overflow anti-stuck-key behavior, diagnostics, shared USB descriptor golden vectors, keyboard/mouse serialization, and a host-testable callback lifecycle state. The T03 cold-boot rework adds physical-snapshot observation and a release-confirmation barrier inside the same runtime: input remains suppressed while a boot-time key is held, and unlock requires completion of a zero report produced after physical release. The lifecycle queue exposes all 16 promised slots; overflow is counted and reconciled fail-safe against the callback snapshot. No Maker code copied. |
| main/main.cpp | ESP-IDF v5.5.5 GPIO, FreeRTOS, esp_timer and esp_tinyusb APIs, Apache-2.0; Maker fixed commit used only for lifecycle review | Independent ESP32-S3 adapter with GPIO any-edge ISR, static 64-item raw-edge queue, owner task, debounced physical-mask publication, incomplete input-sequence discard before recovery, ordered bounded TinyUSB lifecycle event queue with epoch-gated completion/failure, shared host-testable ready/send-attempt helpers, duplicate-mount-stable callback epochs, classified overflow recovery logging, callbacks limited to publication/notification, and shared descriptor transport. GPIO0, GPIO8, audio and J4 UART absent. |
| main metadata, dependencies.lock and sdkconfig.defaults | ESP-IDF v5.5.5 Component Manager metadata; esp_tinyusb 1.7.6~2 and tinyusb 0.21.0~1 | Explicit managed dependency and reproducible lock metadata; no managed_components or generated sdkconfig committed. |
| partitions.csv, CMake partition guard and Host source-contract vector | EasyInput Maker `7619bd13f9ddfd6e2d80e2b8e022ef0acf32ce01:partitions.csv`, PolyForm Noncommercial 1.0.0; the same layout was independently observed in the authorized 2026-08-25 board backup | Contract data adopted to preserve the existing 24 KiB NVS, 4 KiB PHY, 3 MiB factory app and two 576 KiB sound banks. No storage implementation was copied. Build and Host gates reject layout drift; T03 does not read, initialize or modify NVS or sound-bank contents. |
| host_test files | Frozen task matrix; Maker host-test structure reviewed at fixed commit | Independent stderr/non-zero tests for vectors, ownership, the same production lifecycle processor and callback state (including duplicate mount, stale completion, full 16-slot capacity and overflow reconciliation), complete byte-for-byte device/configuration/language/string/report descriptor vectors plus semantic report parsing, default serialization bytes, event-ring overflow recovery, wheel coalescing/boundaries/no-replay, vendor fail-closed, diagnostics, source boundaries, and fresh InputCore/runtime cold boot with S6 already held. Cold-boot coverage includes delayed HID readiness, accepted/failed/completed transfer attempts, duplicate mount, held-input suppression, release without a Press owner, release before the mount zero completes, and post-barrier Ctrl+C. |

The external Maker checkout remains read-only and is not a product dependency. No Xiaozhi source was used.
