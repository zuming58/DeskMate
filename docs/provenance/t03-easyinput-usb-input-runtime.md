# T03 EasyInput USB input runtime provenance

All T03 implementation files are clean-room reimplementations. No source file, generated file, binary, or build output was copied from an external project.

| Product file | Fixed reference and license | Adoption and target |
| --- | --- | --- |
| input_core include and source files | EasyInput Maker 7619bd13f9ddfd6e2d80e2b8e022ef0acf32ce01, PolyForm Noncommercial 1.0.0; ESP-IDF v5.5.5 Apache-2.0 | Debounce and Gray-code behavior independently reimplemented from the frozen contract; split key/phase/press entry points, bounded event ring, overflow accounting, explicit pending-sequence discard after a drop, encoder resync, and a read-only debounced eight-key physical mask for the existing runtime owner. |
| hid_report include and source files | Maker fixed commit above; PolyForm Noncommercial 1.0.0; HID boot convention | Independent held-key state with modifier + apple_fn + six usages, fail-closed rollover and complete release. |
| input_runtime include and source files | Frozen INPUT_V1_FROZEN contract; Maker `components/keyboard/include/keyboard/transport_routing.h`, `components/keyboard/src/transport_routing.cpp`, `components/keyboard/include/keyboard/keyboard_snapshot_delivery.h`, `components/keyboard/src/keyboard_snapshot_delivery.cpp`, `main/platform/usb_hid.cpp/.h` and keyboard lifetime tests read with `git show` at fixed commit; PolyForm Noncommercial 1.0.0 | Clean reimplementation of default action ownership, encoder axis routing, 16-item HID queue, mount epochs, disconnect/overflow anti-stuck-key behavior, diagnostics, shared USB descriptor golden vectors, keyboard/mouse serialization, and host-testable callback and DCD connection gates. Cold-boot physical snapshots and release confirmation stay inside the same runtime. Every real mount advances epoch, stale events are rejected against the current callback snapshot, and physical recovery never synthesizes a mount. HID completion/failure matches epoch, Report ID, length and payload. This rework independently adds a 25 ms / 500 ms bounded all-released delivery window that starts only after the first zero report completes and remains fail-closed for a held physical key. No Maker code copied. |
| board_pins.h and main/main.cpp | Maker fixed commit `components/keyboard/include/keyboard/board_pins.h`, `main/app_main.cpp` and `main/platform/usb_hid.cpp/.h`, PolyForm Noncommercial 1.0.0; ESP-IDF v5.5.5 `usb_phy.h` and esp_tinyusb 1.7.6~2 `include/tinyusb.h`/`tinyusb.c`, Apache-2.0; tinyusb 0.21.0~1 `src/device/usbd.c` and `src/portable/synopsys/dwc2/dcd_dwc2.c`, MIT | Independently adopted the V2 board fact `GPIO40`, active-low, as the USB/SEN_VIN physical-presence input. Maker uses it only in application lifetime logic and leaves esp_tinyusb `self_powered=false` with no PHY VBUS pin. ESP-IDF's direct B-valid monitor is active-high, so GPIO40 is not passed with inverted semantics. Instead, the single owner task applies the host-tested 25 ms loss filter and explicitly invokes TinyUSB `tud_disconnect()`/`tud_connect()` once per stable physical transition, retrying a failed platform action. Existing edge ISR, static queues, physical key mask, overflow recovery and descriptors remain; GPIO0, GPIO8, audio and J4 UART remain absent. |
| main metadata, dependencies.lock and sdkconfig.defaults | ESP-IDF v5.5.5 Component Manager metadata; esp_tinyusb 1.7.6~2 and tinyusb 0.21.0~1 | Explicit managed dependency and reproducible lock metadata; no managed_components or generated sdkconfig committed. |
| partitions.csv, CMake partition guard and Host source-contract vector | EasyInput Maker `7619bd13f9ddfd6e2d80e2b8e022ef0acf32ce01:partitions.csv`, PolyForm Noncommercial 1.0.0; the same layout was independently observed in the authorized 2026-08-25 board backup | Contract data adopted to preserve the existing 24 KiB NVS, 4 KiB PHY, 3 MiB factory app and two 576 KiB sound banks. No storage implementation was copied. Build and Host gates reject layout drift; T03 does not read, initialize or modify NVS or sound-bank contents. |
| host_test files | Frozen task matrix; Maker host-test structure and physical-presence/lifetime semantics reviewed at fixed commit | Independent stderr/non-zero tests for vectors, ownership, lifecycle processing, complete descriptors, serialization, overflow recovery, wheel no-replay, vendor fail-closed, diagnostics and source boundaries. Reconnect coverage includes GPIO40 active-low/25 ms exact boundary and millisecond wrap, DCD connect/disconnect action de-duplication and retry, delayed HID readiness that cannot age out the 500 ms release window, 25 ms release interval, raw-loss fail-closed recovery, two complete reconnect cycles without TinyUSB unmount, held S6 suppression, release without a Press owner, stale Ctrl and same-byte stale zero completion/failure, wrong identity rejection, and post-barrier Ctrl+C. |

The external Maker checkout remains read-only and is not a product dependency. No Xiaozhi source was used.
