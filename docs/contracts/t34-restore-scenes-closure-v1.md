# T34 software reliability closure

Status: SOFTWARE_RELIABILITY_CLOSURE_V1_FROZEN

- Restore uses an immutable staged backup and expiring token. Native confirmation names replacement scope; reject while voice, writes, cleanup, summary or sync is busy. Acquire maintenance before queueing; restart applies before opening databases, hardware or renderer effects. Failed/interrupted apply rolls back or blocks startup.
- Renderer adopts restored configuration before React mounts, preserves a previous browser snapshot and acknowledges only after durable local writes. Disable automatic wake, shortcuts, cleanup, memory scheduling and KnowledgeOS access until settings are reviewed. Preserve credentials locally but never include them in backup. Imported days stay excluded from automatic summary/submission, including after normal sync is re-enabled. New days remain eligible.
- Scenes retain stable IDs, bindings, prompt order, user title/description and archive state. Only active scenes participate in Tab. At least one active scene remains. Changes require expected revision; conflicts preserve drafts. KEY1/2/3/4/8 remain shared; KEY5/6/7 remain scene-local.
- Memory results precede advanced policy/connection/maintenance controls. Manual hardware testing and Codex automatic status must be distinctly labeled. Draft cancellation is explicit, asynchronous actions expose errors and block repeated clicks.
- Validate with isolated profiles, interrupted restore, no-replay, scene archive/order/conflict tests, responsive UI and packaged resources. User data and real hardware are reserved for user acceptance.
